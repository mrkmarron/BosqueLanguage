{
    "suite": "readme",
    "tests": [
        {
            "test": "add",
            "src": null,
            "sig": "function main(x: Nat, y: Nat): Bool",
            "code": "function add2(x: Nat, y: Nat): Nat { return x + y; }",
            "refutes": [
                "add2(x, y) >= 0n",
                "add2(x, 0) == x",
                "add2(0, y) == y"
            ],
            "reachables": [
                "add2(1, 2) == 3n",
                "add2(x, y) == 3n"
            ]
        }
    ]
}